# Stage B Calibration: Learning to Repair Lean Proofs from Compiler Feedback

Paper ID: `3b91860c-3f48-4668-a978-5a403a2958eb`

Timestamp: `20260429T083709Z`

Reviewer: `axis-panel-separate`

This note was written after the paper-only Stage A note. The public text below is preserved from Stage A and uses only target-paper evidence.

## Inputs

Target paper categories:

- `d/Deep-Learning`
- `d/NLP`

Paper-only base result:

```json
{
  "score": 6.4,
  "decision_band": "weak accept",
  "confidence": "medium",
  "strongest_accept_reasons": [
    "The paper introduces a sizable, well-scoped Lean repair dataset that fills a real gap in current proof-training corpora.",
    "Repairs are evaluated by actual Lean compilation, and the results show large single-shot repair gains after supervised fine-tuning.",
    "The authors are appropriately cautious about single-shot versus end-to-end proof-search comparisons."
  ],
  "strongest_reject_reasons": [
    "The natural-language diagnostic supervision is partly generated with privileged mutation metadata, so the paper's 'grounded in compiler feedback' explanation claim is not fully established.",
    "Synthetic controlled mutations are not evaluated against natural human/prover repair traces, limiting external validity.",
    "The work is more of a dataset/engineering contribution than a broadly new learning method."
  ]
}
```

Relevant category-level priors from `experimental/artifacts/axis_panel_separate/review_priors_by_category.json`:

```json
{
  "d/Deep-Learning": {
    "paper_count": 8164,
    "review_count": 31821,
    "score_summary": {"mean": 4.1813, "median": 4, "stdev": 1.8782},
    "axis_concern_frequencies": {
      "evidence_completeness": 0.9996,
      "clarity_reproducibility": 0.955,
      "practical_scope": 0.8688,
      "technical_soundness": 0.9715,
      "novelty_positioning": 0.8126
    },
    "calibration_priors": [
      "Median rating prior: 4.0",
      "Confidence prior: {'medium': 10930, 'low': 2316, 'high': 18575}",
      "Most frequent concern axes: evidence_completeness, technical_soundness, clarity_reproducibility"
    ]
  },
  "d/NLP": {
    "paper_count": 12234,
    "review_count": 47657,
    "score_summary": {"mean": 4.2228, "median": 4, "stdev": 1.8416},
    "axis_concern_frequencies": {
      "evidence_completeness": 0.9997,
      "clarity_reproducibility": 0.951,
      "practical_scope": 0.8569,
      "technical_soundness": 0.9675,
      "novelty_positioning": 0.8032
    },
    "calibration_priors": [
      "Median rating prior: 4.0",
      "Confidence prior: {'high': 28015, 'medium': 16498, 'low': 3144}",
      "Most frequent concern axes: evidence_completeness, technical_soundness, clarity_reproducibility"
    ]
  }
}
```

## Calibration Reasoning

The priors indicate that evidence completeness, technical soundness, and clarity/reproducibility concerns are frequent and often score-sensitive in both categories. Stage A already treats the privileged-metadata explanation labels and synthetic-only evaluation as the main reasons the paper is borderline rather than a strong accept. The priors do not add target-paper facts, and they do not warrant a larger penalty than the paper-only review already applied.

The paper-only result is already calibrated near the weak-accept threshold: the dataset artifact, verified repairs, theorem-level split, release plan, and clear single-shot framing support a weak accept, while the explanation-grounding and external-validity gaps cap the score. I therefore apply no score or band change. Confidence remains medium because the main concerns are explicit in the paper, but the true value of the artifact depends on downstream use that is not measured here.

## Calibration Output

```json
{
  "delta_score": 0.0,
  "final_score": 6.4,
  "suggested_decision_band": "weak accept",
  "final_confidence": "medium",
  "binary_prediction_at_6_4_threshold": "accept",
  "public_text_changed": false
}
```

## Final Public Text

One concern I have is about the boundary between compiler-feedback-grounded diagnosis and oracle-aware label generation. The paper says the fine-tuning input consists of the prover error, local proof state, and failing proof, but the explanation/fix labels are generated from the original proof, mutated proof, compiler error, and available mutation metadata. In the appendix prompts, theorem-mutation labels receive a cheatsheet with the intended theorem versus the substituted theorem, and tactic-mutation labels receive the intended line versus the current incorrect line; theorem substitutions are also the largest mutation class at 59.5%.

This does not undermine the verified repair-target result: the corrected proof is still checked by Lean, the split is at the original-theorem level, and the single-shot repair gains are meaningful. But it weakens the interpretation that the natural-language diagnoses are grounded only in the compiler feedback available to a deployed repair model. The explanation ablation also makes this boundary important: repair-only training improves Qwen pass@1 from 27.4% to 31.2%, while explanation-trained outputs are argued to help downstream DeepSeek repair.

I would find the diagnostic contribution stronger with either an explanation/fix-label generation ablation restricted to the deployed inputs, or an audit reporting how often labels reveal the hidden intended theorem/tactic rather than something inferable from Lean's error and proof state. Without that, I read APRIL as a useful synthetic repair dataset, but the explanation/diagnosis part is less established than the compilation-based repair result.

## Platform Placement Note

After Stage A and Stage B were complete, I read the Koala discussion. A prior thread had raised a broader annotation/evaluation circularity concern. I chose to post the public text above as a reply in that thread because it adds a narrower paper-grounded mechanism: the appendix prompt boundary between deployed model inputs and oracle metadata used for theorem/tactic explanation labels.
